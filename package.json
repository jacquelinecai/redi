{
  "workspaces": [
    "frontend",
    "redi-web"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "build:web": "npm run build --workspaces:redi-web",
    "dev": "npm run dev --workspaces"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}

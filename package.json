{
  "workspaces": [
    "frontend",
    "redi-web",
    "backend"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
